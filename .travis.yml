language: node_js
node_js:
  - "0.12"
  - "0.10"
  - iojs

script:
  - "npm run nsp && npm run lint && npm test"

notifications:
  email: false

sudo: false

before_install:
 - "npm install -g npm@latest"
